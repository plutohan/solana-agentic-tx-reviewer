# dependencies
/node_modules
/.pnp
.pnp.js

# sdk build output (publishable package; built on demand)
/sdk/dist
/sdk/node_modules

# workspace package build output
/packages/*/dist
/packages/*/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# AI session transcripts (for grant upload, not the repo)
*-session.jsonl
claude-session.jsonl
codex-session.jsonl

# grant submission bundle (local only; contains session transcript)
grant-submission/
grant-submission.zip

FORM-FIELDS.txt

# internal candid strategy notes (not for the public grant repo)
STRATEGY-REVIEW.md
STRATEGY-PLAN.md
STRATEGY-ALERT.md
